{
  "name": "ecom-shared-packages",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "packageManager": "yarn@4.15.0",
  "scripts": {
    "build": "turbo run build",
    "type-check": "turbo run type-check",
    "clean": "turbo run clean && rm -rf node_modules",
    "storybook": "yarn workspace @flash-ship/ecom-ui storybook",
    "build-storybook": "yarn workspace @flash-ship/ecom-ui build-storybook",
    "yalc:publish:all": "npx yalc publish packages/ui --private --push && npx yalc publish packages/i18n --private --push && npx yalc publish packages/lib --private --push && npx yalc publish packages/types --private --push && npx yalc publish packages/config --private --push && npx yalc publish packages/emails --private --push && npx yalc publish packages/tsconfig --private --push && npx yalc publish packages/trpc-types --private --push",
    "yalc:publish:ui": "npx yalc publish packages/ui --private --push",
    "script:migrate-ui": "node scripts/migrate-ui-imports.js",
    "script:cleanup-ui": "node scripts/cleanup-local-ui.js",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/cli": "^2.27.1",
    "turbo": "^2.5.0",
    "typescript": "^5.8.0"
  }
}
